# Copy to .env.local and fill in.
# Linear personal API key: Linear -> Settings -> Security & access -> Personal API keys
LINEAR_API_KEY=

# Optional: port for the local server (default 8787)
PORT=8787
